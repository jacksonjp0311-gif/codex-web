# CODEX RootMirror (Module Stub)

RootMirror is the *git continuity* step:
- stage canonical subtree
- commit (append-only lineage)
- pull --rebase
- push

This module is operational glue, not THOTH semantics.
